<p id="notice"><%= notice %></p>

<p>
<strong>Номер накладной:</strong>
<%= @package.tracking_number %>
</p>

<p>
<strong>Курьер, который доставит посылку:</strong>

<% if @package.courier_on_the_way.present?%>
<%= @package.courier_on_the_way.name%>
<% else %>
Не указано
<% end %>
</p>

<p>
<strong>Статус доставки:</strong>
<%= @package.delivery_status %>

<br><br><br>
<%= link_to 'Редактировать', edit_package_path(@package) %> |
<%= link_to 'Назад', packages_path %>
